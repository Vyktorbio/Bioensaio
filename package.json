{
  "name": "bioensaio",
  "version": "0.1.0",
  "private": true,
  "description": "Estatística inteligente para bioensaios (probit/logit, ANOVA, GLM, Tukey/Scott-Knott)",
  "scripts": {
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug",
    "build:apk:release": "cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
